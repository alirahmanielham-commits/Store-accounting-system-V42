   475	                                  }} className="w-full border border-slate-200 bg-white rounded-lg p-2.5 outline-none focus:border-indigo-500 text-sm text-left" dir="ltr" placeholder={comp.formula || 'مثال: B * 0.1'} />
   476	                                </div>
   477	                              )}
   478	                            </div>
   479	                          )}
   480	                        </div>
   481	                      );
   482	                    })}
   483	                  </div>
   484	                </div>
   485	              )}
   486	
   487	              <div className="flex items-center gap-3">
   488	                <div className="w-10 h-10 rounded-xl bg-indigo-100 text-indigo-600 flex items-center justify-center"><FileSignature className="w-5 h-5"/></div>
   489	                <h3 className="font-bold text-slate-800 text-lg">{editingContractId ? 'ویرایش قرارداد پرسنل' : 'ثبت قرارداد جدید'}</h3>
   490	              </div>
   491	              <button onClick={() => setIsContractModalOpen(false)} className="text-slate-400 hover:text-slate-600 p-2 bg-white rounded-lg border border-slate-200"><X className="w-5 h-5"/></button>
   492	            </div>
   493	
   494	            {/* Stepper Header */}
   495	            <div className="bg-white border-b border-slate-100 px-6 py-4 flex items-center justify-between">
   496	               <div className="flex items-center gap-4 w-full">
   497	                 <div className={`flex items-center gap-2 ${wizardStep >= 1 ? 'text-indigo-600' : 'text-slate-400'}`}>
   498	                   <div className={`w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm ${wizardStep >= 1 ? 'bg-indigo-600 text-white' : 'bg-slate-100 text-slate-500'}`}>1</div>
   499	                   <span className="font-bold text-sm hidden sm:block">انتخاب پرسنل</span>
   500	                 </div>
   501	                 <div className={`flex-1 h-1 rounded-full ${wizardStep >= 2 ? 'bg-indigo-600' : 'bg-slate-100'}`}></div>
   502	                 <div className={`flex items-center gap-2 ${wizardStep >= 2 ? 'text-indigo-600' : 'text-slate-400'}`}>
   503	                   <div className={`w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm ${wizardStep >= 2 ? 'bg-indigo-600 text-white' : 'bg-slate-100 text-slate-500'}`}>2</div>
   504	                   <span className="font-bold text-sm hidden sm:block">اطلاعات قرارداد</span>
   505	                 </div>
   506	                 <div className={`flex-1 h-1 rounded-full ${wizardStep >= 3 ? 'bg-indigo-600' : 'bg-slate-100'}`}></div>
   507	                 <div className={`flex items-center gap-2 ${wizardStep >= 3 ? 'text-indigo-600' : 'text-slate-400'}`}>
   508	                   <div className={`w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm ${wizardStep >= 3 ? 'bg-indigo-600 text-white' : 'bg-slate-100 text-slate-500'}`}>3</div>
   509	                   <span className="font-bold text-sm hidden sm:block">تخصیص اجزا</span>
   510	                 </div>
   511	               </div>
   512	            </div>
   513	            
   514	            <div className="p-6 overflow-y-auto custom-scrollbar flex-1 bg-slate-50/50">
   515	              
   516	              {/* Step 1: Employees Selection */}
   517	              {wizardStep === 1 && (
   518	                <div className="space-y-6 animate-in fade-in slide-in-from-right-4 duration-300">
   519	                  <div className="bg-white border border-slate-200 rounded-2xl p-6 shadow-sm w-[840px] max-w-full mx-auto">
   520	                    <h4 className="font-bold text-slate-800 mb-4 flex items-center gap-2">
   521	                      <Users className="w-5 h-5 text-indigo-500" />
   522	                      انتخاب پرسنل جهت انتساب قرارداد
   523	                    </h4>
   524	                    <p className="text-sm text-slate-500 mb-6">شما می‌توانید مستقیم کارمند مورد نظر خود را برای ثبت قرارداد انتخاب کنید. <span className="font-bold text-indigo-600">تنها افرادی که دارای نقش «کارمند» هستند در لیست نمایش داده می‌شوند.</span></p>
   525	                    <div className="grid grid-cols-1 gap-6">
   526	                      <div>
   527	                        <label className="block text-sm font-bold text-slate-700 mb-2">انتخاب کارمند</label>
   528	                        <Select
   529	                          isDisabled={!!editingContractId}
   530	                          options={employees.map(p => ({value: p.id, label: p.name}))}
   531	                          value={contractForm.personId}
   532	                          onChange={v => {
   533	                            const newPerson = v;
   534	                            const updates: any = { personId: newPerson };
   535	                            if (newPerson && !editingContractId) {
   536	                              const selectedPerson = employees.find(p => p.id === newPerson.value);
   537	
   538	                            }
   539	                            setContractForm({...contractForm, ...updates});
   540	                          }}
