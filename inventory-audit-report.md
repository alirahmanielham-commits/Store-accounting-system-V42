# گزارش ممیزی فنی و مالی ماژول انبارداری و مدیریت موجودی کالا

به عنوان یک ممیز ارشد سیستم‌های ERP و متخصص در حوزه بهای تمام‌شده و انبارداری، سورس‌کد سیستم (به‌ویژه لایه‌های `inventoryService.ts`، `system.routes.ts`، `StocktakingManager.tsx` و روال‌های `useAppController.tsx`) بررسی شد.

معماری فعلی بر اساس الگوی **موجودی دو-مرحله‌ای (Decoupled Inventory)** طراحی شده است (یعنی فاکتور مالی از سند انبار جداست)، اما این الگو به صورت ناقص و متناقض پیاده‌سازی شده و از نظر اصول حسابداری صنعتی فاقد موتور هزینه‌یابی (Costing Engine) است.

در ادامه، گزارش ممیزی فنی و مالی در قالب جداول تدوین شده است:

### ۱. خلاصه اجرایی ممیزی
سیستم فعلی برای کسب‌وکارهای کوچک با حجم تراکنش پایین و بدون نیاز به حسابداری صنعتی دقیق کار می‌کند، اما در محیط‌های Enterprise با تراکنش همزمان (High Concurrency) و نیاز به محاسبه دقیق بهای تمام‌شده کالای فروش‌رفته (COGS)، دچار **فروپاشی داده (Data Inconsistency)** و **خطای ارزش‌گذاری** خواهد شد. بزرگترین ریسک‌های سیستم، فقدان موتور FIFO/میانگین موزون، و روش مخربِ بازنویسی کامل دیتابیس (Full Table Truncate) در هر تراکنش انبار است.

---

### ۲. جدول باگ‌ها (یکپارچگی، امنیت و معماری)

| ردیف | عنوان باگ | توضیح فنی | سناریوی بازتولید | شدت | فایل/بخش مرتبط |
|:---:|:---|:---|:---|:---:|:---|
| **۱** | آسیب‌پذیری در همزمانی (Race Condition) | اعتبارسنجی «موجودی منفی» فقط در فرانت‌اند (`useAppController.tsx`) انجام می‌شود. بک‌اند هیچ اعتبارسنجی روی موجودی ندارد. | دو کاربر همزمان یک کالا با موجودی ۱ عدد را در دو فاکتور جداگانه می‌فروشند. سیستم هر دو را ثبت کرده و موجودی منفی ۱ می‌شود. | **بحرانی (Critical)** | `useAppController.tsx` / `data.routes.ts` |
| **۲** | بازنویسی مخرب جدول موجودی (TRUNCATE) | تابع `recalculateAllWarehouseStocks` برای محاسبه موجودی، کل جدول را Truncate کرده و مجدد می‌سازد. | ثبت هر سند انبار باعث اجرای این روال می‌شود که در سیستم‌های با تراکنش بالا، منجر به Database Lock می‌شود. | **بحرانی (Critical)** | `system.routes.ts` / `recalculateAllWarehouseStocks` |
| **۳** | گسست در چرخه انبارگردانی (Dead End) | ماژول انبارگردانی صرفاً یک ماشین‌حساب مغایرت‌گیری است و با کلیک روی «اعمال»، سند اصلاحی تولید نمی‌کند. | کاربر انبارگردانی را تایید می‌کند، سیستم پیغام می‌دهد اسناد را دستی ثبت کنید اما وضعیت انبارگردانی معلق می‌ماند. | **مهم (High)** | `StocktakingManager.tsx` |
| **۴** | تناقض در الگوی دو-مرحله‌ای (Decoupled) | برای فروش کالا `reservedStock` افزایش می‌یابد، اما برای فاکتور خرید، برگشت از فروش و ضایعات هیچ متغیری (مثل `expectedStock`) افزایش نمی‌یابد. | کاربر فاکتور خرید ثبت می‌کند، اما انباردار هیچ درخواستی برای صدور «رسید انبار» در کارتابل خود نمی‌بیند. | **مهم (High)** | `system.routes.ts` / `invoiceService.ts` |

---

### ۳. جدول مشکلات محاسباتی (هزینه‌یابی و بهای تمام‌شده)

| ردیف | نوع محاسبه | مشکل | مثال عددی (قبل/بعد اصلاح) | تاثیر مالی احتمالی |
|:---:|:---|:---|:---|:---|
| **۱** | الگوریتم هزینه‌یابی | **فقدان مطلق موتور محاسبه بهای تمام‌شده (Cost Engine)**. هیچ اثری از FIFO، LIFO یا میانگین موزون نیست. | خرید ۱: ۱۰ عدد ۱۰۰۰ تومان. خرید ۲: ۵ عدد ۱۵۰۰ تومان. فروش: ۱۲ عدد. بهای تمام‌شده در سیستم فعلی ۰ یا نامشخص است (باید ۱۷۵۰۰ باشد). | سود و زیان (P&L) به شدت غیردقیق و غیرقابل اتکا خواهد بود و مالیات اشتباه محاسبه می‌شود. |
| **۲** | ارزش‌گذاری انبارگردانی | **استفاده از نرخ ثابت برای ارزش‌گذاری کسری/اضافی** بر اساس یک متغیر ثابت (`p.purchasePrice` یا `p.price`). | کالایی با میانگین قیمت ۱۲۰۰ تومان کسری خورده است. سیستم کسری را بر اساس آخرین قیمت خرید (۱۵۰۰) ثبت می‌کند. | کسری‌ها و اضافات انبار مغایر با استانداردهای حسابداری (مانند IAS 2) ارزش‌گذاری می‌شوند. |
| **۳** | ارتباط فاکتور و انبار | **عدم اثرگذاری اتوماتیک فاکتور برگشت و ضایعات** در حلقه بازیافت دیتابیس موجودی. | یک فروش برگشت می‌خورد، اما به موجودی فیزیکی/مقداری سیستم اضافه نمی‌شود. | موجودی فیزیکی و ارزش موجودی پایان دوره با دفاتر مالی مغایرت دائمی خواهد داشت. |

---

### ۴. جدول پیشنهادهای ارتقا

| ردیف | عنوان ارتقا | دلیل نیاز | میزان اهمیت | پیچیدگی پیاده‌سازی |
|:---:|:---|:---|:---:|:---:|
| **۱** | ایجاد موتور بهای تمام‌شده (FIFO / Average) | محاسبه دقیق COGS با ایجاد جدول `inventory_layers` برای رهگیری لایه‌های قیمتی موجودی. | بسیار بالا | زیاد |
| **۲** | جایگزینی Truncate با Delta Updates | جلوگیری از قطعی سیستم و ارتقای پرفورمنس با آپدیت تفاضلی (Transaction-based) مقادیر انبار. | بسیار بالا | متوسط |
| **۳** | انتقال اعتبارسنجی موجودی به بک‌اند | جلوگیری از بروز موجودی منفی همزمان (Concurrency Issues) از طریق Row-level Locking. | بسیار بالا | کم |
| **۴** | اتوماسیون صدور اسناد انبارگردانی | به حداقل رساندن خطای انسانی در ثبت دستی اسناد کسری و اضافی پس از اعمال انبارگردانی. | بالا | متوسط |
| **۵** | رهگیری موجودی در راه (Pending) | اضافه کردن فیلدهای `pending_receipt` و `pending_return` برای اطلاع‌رسانی به انباردار. | متوسط | متوسط |

---

### ۵. اولویت‌بندی نهایی برای رفع مشکلات (بر اساس ریسک مالی و فوریت فنی)

۱. **فوری و حیاتی:** انتقال اعتبارسنجی موجودی منفی به بک‌اند برای جلوگیری از فساد داده در حالت Concurrency.
۲. **فوری و حیاتی:** جایگزینی سیستم بازنویسی کل جدول (Truncate) با آپدیت‌های دلتا (رویداد-محور).
۳. **خیلی بالا:** طراحی و پیاده‌سازی موتور هزینه‌یابی (FIFO/میانگین موزون) و صدور اسناد بهای تمام‌شده کالای فروش رفته (COGS).
۴. **بالا:** توسعه روتر جهت صدور خودکار اسناد اصلاحی در ماژول انبارگردانی.
۵. **متوسط:** رهگیری مقادیر فاکتورهای خرید، برگشت و ضایعات به عنوان موجودی معلق (Pending).
