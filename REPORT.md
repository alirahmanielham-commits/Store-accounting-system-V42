## ۱. خلاصه اجرایی
سیستم مدیریت حسابداری و وام بررسی شد. پروژه دارای معماری Full-Stack (Node.js/Express + React) است. با وجود پوشش امکانات گسترده، پروژه در لایه بک‌اند و ارتباط با دیتابیس دارای بدهی فنی (Technical Debt) بسیار بالا و ریسک‌های امنیتی و کارایی (Performance) است. بزرگترین خطرات فعلی شامل کوئری‌های داینامیک بدون اعتبارسنجی دقیق، بارگیری کامل جداول در حافظه (بدون Pagination) و نرخ برابری ارز هاردکد شده (Hardcoded) است که نیازمند اصلاح فوری پیش از استقرار در محیط عملیاتی می‌باشند.

## ۲. باگ‌ها و خطاهای عملکردی
- **تولید شناسه‌های تصادفی تکراری برای اقساط**
  - **فایل:** `src/utils/installmentUtils.ts` (تابع `generateInstallmentCode`)
  - **مشکل:** استفاده از `Math.random()` برای تولید کد یکتا.
  - **سناریو بروز:** هنگام صدور دفترچه اقساط برای وام‌های طولانی‌مدت، احتمال بروز Collision و اختلال در سیستم پرداخت.
  - **شدت:** High
  - **راهکار:** استفاده از کتابخانه `uuid` یا ترکیب `Date.now()` با یک رشته تصادفی ایمن (Crypto).
- **تولید شناسه (ID) ناامن برای رکوردها**
  - **فایل:** `server.ts` (در اندپوینت‌های `/api/data/:key/append` و تابع `handleRelations`)
  - **مشکل:** استفاده از `Math.random().toString(36).substring(2, 15)`.
  - **شدت:** High
  - **راهکار:** جایگزینی با `crypto.randomUUID()`.

## ۳. مشکلات منطقی
- **خطا در پارس کردن تاریخ‌های جلالی/میلادی**
  - **فایل:** `src/utils/format.ts` (تابع `convertToGregorian`)
  - **مشکل:** تبدیل دستی و Regex-محور تاریخ‌ها بدون در نظر گرفتن دقیق Timezoneها.
  - **سناریو بروز:** تغییر ساعت سیستم کلاینت می‌تواند باعث شیفت شدن تاریخ سررسید چک‌ها یا اقساط به روز قبل یا بعد شود.
  - **شدت:** Medium
  - **راهکار:** استفاده یکپارچه از کتابخانه‌های معتبر مانند `date-fns-jalali` یا `moment-jalaali`.
- **ایجاد حساب کاربری ادمین پیش‌فرض ناامن**
  - **فایل:** `server.ts` (تابع `getUsers`)
  - **مشکل:** اگر دیتابیس خالی باشد، یک ادمین با پسورد هاردکد شده `admin` ساخته می‌شود.
  - **شدت:** Critical
  - **راهکار:** سیستم در اولین اجرا باید کاربر را به صفحه Setup هدایت کرده و رمز عبور قوی دریافت کند، نه اینکه رمز `admin` ثبت کند.

## ۴. خطاهای محاسباتی
- **هاردکد شدن نرخ برابری ارزها**
  - **فایل:** `src/utils/format.ts` (تابع `getBaseValueInToman`)
  - **مشکل:** نرخ دلار ۷۰,۰۰۰ و یورو ۷۵,۰۰۰ تومان در کد Hardcode شده است.
  - ```typescript
    if (cur.includes('دلار') || cur.includes('USD')) return 70000;
    ```
  - **سناریو بروز:** هرگونه ثبت سند ارزی با این توابع، گزارش‌های مالی و حسابداری را کاملاً اشتباه و غیرقابل اعتماد می‌کند.
  - **شدت:** Critical
  - **راهکار:** پیاده‌سازی جدول `exchange_rates` در دیتابیس و مدیریت پویای نرخ ارز توسط کاربر.

## ۵. مشکلات امنیتی
- **خطر SQL Injection و Dynamic DDL**
  - **فایل:** `server.ts` (توابع `innerSetDbData` و بررسی جداول)
  - **مشکل:** اجرای کوئری `CREATE TABLE IF NOT EXISTS "${key}"` با پارامترهای دریافت‌شده از کلاینت. با اینکه آرایه `KNOWN_TABLES` تا حدودی جلوی اکسپلویت را می‌گیرد، این یک الگوی ضدامنیتی (Anti-Pattern) شدید است.
  - **شدت:** High
  - **راهکار:** استفاده از ORM (مانند Drizzle که در پروژه وجود دارد اما کامل متصل نیست) و سیستم Migration ساختاریافته.
- **کلیدهای JWT هاردکد شده**
  - **فایل:** `server.ts`
  - **مشکل:** در صورت فقدان متغیر محیطی، از مقدار ثابت `'super-secret-jwt-key-2024'` استفاده می‌شود.
  - **شدت:** High
  - **راهکار:** توقف کامل اجرای سرور (Throw Error) در صورت عدم وجود `JWT_SECRET` در `.env`.

## ۶. مشکلات کارایی
- **بارگیری کامل داده‌ها (عدم وجود Pagination)**
  - **فایل:** `server.ts` (تابع `innerGetDbData`)
  - **مشکل:** اجرای `SELECT * FROM "table"` برای خواندن موجودیت‌ها.
  - **سناریو بروز:** با افزایش تعداد تراکنش‌ها یا اقساط، حافظه سرور پر شده (OOM) و برنامه کند یا متوقف می‌شود.
  - **شدت:** Critical
  - **راهکار:** پیاده‌سازی Pagination (Limit/Offset) در سمت سرور و کلاینت.
- **رندرهای تکراری و سنگین در React**
  - **فایل:** `src/components/inventory/OrderList.tsx` و سایر کامپوننت‌های لیست
  - **مشکل:** استفاده از `filter` و `map` مستقیماً در JSX بدون استفاده از `useMemo`.
  - **شدت:** Medium
  - **راهکار:** کش کردن نتایج فیلتر با `useMemo`.

## ۷. مشکلات ساختاری کد
- **نقض شدید قانون Single Responsibility (SOLID)**
  - **فایل:** `server.ts`
  - **مشکل:** یک فایل بیش از ۲۵۰۰ خطی که مسئول مسیریابی، ارتباط با دیتابیس، آپدیت شمای SQL، مدیریت بکاپ‌ها و احراز هویت است.
  - **شدت:** High
  - **راهکار:** ماژولار کردن سرور به پوشه‌های `routes`, `controllers`, `models`, `middlewares`.
- **وابستگی کلاینت به رویدادهای Global**
  - **فایل:** `src/services/coreService.ts`
  - **مشکل:** استفاده از `window.dispatchEvent(new CustomEvent('app_data_changed'))` برای همگام‌سازی Stateها به جای استفاده اصولی از React Query (Invalidation).

## ۸. جدول جمع‌بندی و اولویت‌بندی

| مشکل | شدت | فایل | اقدام پیشنهادی |
|---|---|---|---|
| نرخ هاردکد شده ارزها | Critical | `format.ts` | حذف توابع استاتیک ارزی و استفاده از دیتابیس برای نرخ‌ها |
| پسورد پیش‌فرض admin | Critical | `server.ts` | اجبار کاربر به تعیین پسورد در صفحه Setup اولیه |
| بارگیری جداول بدون Limit | Critical | `server.ts` | اضافه کردن Pagination به APIها |
| کوئری‌های داینامیک DDL | High | `server.ts` | انتقال کامل لاجیک به Drizzle ORM و حذف کوئری‌های رشته‌ای |
| تولید ID با Math.random | High | `server.ts` / `installmentUtils.ts` | استفاده از `crypto.randomUUID()` |
| فایل غول‌پیکر ۲۵۰۰ خطی سرور | High | `server.ts` | Refactor و شکستن به ماژول‌های مجزا |
| پارس دستی تاریخ‌ها | Medium | `format.ts` | استفاده از `date-fns-jalali` |
| فیلترینگ آرایه در Render | Medium | `OrderList.tsx` | پیاده‌سازی `useMemo` |
